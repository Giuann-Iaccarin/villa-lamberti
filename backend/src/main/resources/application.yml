server:
  port: 8080

spring:
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

villa-lamberti:
  contact-email: ${CONTACT_EMAIL:info@villalamberti.it}
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
  whatsapp-number: "+393331234567"

management:
  endpoints:
    web:
      exposure:
        include: health
